name: Test

on:
  pull_request:
    types: [opened, reopened, synchronize, edited]
  workflow_call:

jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: "**/package-lock.json"

      - name: Install Node.js dependencies
        run: npm ci

      - name: Install Node.js dependencies for opensearch plugin
        working-directory: plugins/opensearch-source-plugin
        run: npm ci

      - name: Build Node.js dependencies for opensearch plugin
        working-directory: plugins/opensearch-source-plugin
        run: npm run build

      - name: Run npm build
        run: npm run build
        env:
          SITE_URL: http://localhost:8000
          EUPHROSYNE_GRAPHQL_ENDPOINT: https://euphrosyne.incubateur.net/graphql
          TYPEGEN_ON_BUILD: true
          OPENSEARCH_HOST: http://localhost:9200 # TODO change to the real opensearch host

      - name: Run eslint
        run: npm run lint

      - name: Run prettier
        run: ./node_modules/.bin/prettier --check .

      - name: Run tsc
        run: npm run typecheck
